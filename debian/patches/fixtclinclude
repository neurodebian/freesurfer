--- a/fsgdf/Makefile.am
+++ b/fsgdf/Makefile.am
@@ -10,11 +10,9 @@
 # fsgdf_wrap.c is built by tksurfer and qdec.
 #
 
-if ENABLE_TCLTK_APPS
+# We need the TCL headers even if we're not compiling the
+# app as fsgdf_wrap.c includes tcl.h
 AM_CPPFLAGS=-I$(top_srcdir)/include $(TCL_CFLAGS)
-else
-AM_CPPFLAGS=-I$(top_srcdir)/include
-endif
 AM_LDFLAGS=
 
 lib_LTLIBRARIES= libfsgdf.la
