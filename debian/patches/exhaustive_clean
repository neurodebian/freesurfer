From: Michael Hanke <mih@debian.org>
Subject: Let clean do it properly
--- a/GEMS/Makefile.am
+++ b/GEMS/Makefile.am
@@ -920,6 +920,9 @@ clean-local:
 	rm -f kvlAtlasMeshViewingConsoleGUI.cxx kvlImageThresholdingConsoleGUI.cxx \
 	kvlImageViewingConsoleGUI.cxx kvlSegmentationEvaluationConsoleGUI.cxx
 
+clean-local:
+	-rm kvlImageThresholdingConsoleGUI.h kvlImageViewingConsoleGUI.h
+
 # Our release target. Include files to be excluded here. They will be
 # found and removed after 'make install' is run during the 'make
 # release' target.
--- a/orient_mri/Makefile.am
+++ b/orient_mri/Makefile.am
@@ -163,3 +163,8 @@ vtkOrientMRIInteractorStyleView3D.h
 # release' target.
 EXCLUDE_FILES=
 include $(top_srcdir)/Makefile.extra
+
+clean-local:
+	-rm *Tcl.cxx OrientMRILibInit.cxx
+
+
--- a/qdec/Makefile.am
+++ b/qdec/Makefile.am
@@ -92,6 +92,9 @@ install-data-local:
 uninstall-hook:
 	rm -f $(DESTDIR)$(bindir)/qdec.bin
 
+clean-local:
+	-rm *Tcl.cxx
+
 else
 bin_PROGRAMS = 
 
--- a/scuba2/Makefile.am
+++ b/scuba2/Makefile.am
@@ -223,6 +223,9 @@ install-exec-hook:scuba2
 uninstall-hook:
 	rm -f $(DESTDIR)$(bindir)/scuba2.bin
 
+clean-local:
+	-rm *Tcl.cxx
+
 else
 bin_PROGRAMS = 
 
--- a/vtkutils/Makefile.am
+++ b/vtkutils/Makefile.am
@@ -189,3 +189,6 @@ IconLoaderTest.cxx IconLoaderTestIcons.t
 # release' target.
 EXCLUDE_FILES=
 include $(top_srcdir)/Makefile.extra
+
+clean-local:
+	-rm *Tcl.cxx
